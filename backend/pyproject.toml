[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
# Default set (pyflakes + a slice of pycodestyle) plus import sorting.
select = ["E", "F", "I"]
ignore = ["E501"]  # long lines are common in this codebase's docstrings/urls; not worth failing CI over
