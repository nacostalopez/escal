POSTGRES_USER=escal
POSTGRES_PASSWORD=escal
POSTGRES_DB=escal

# Used by the backend when run outside docker-compose
DATABASE_URL=postgresql+psycopg2://escal:escal@localhost:5432/escal

# Auth — change both in any non-local environment
JWT_SECRET=change-me-in-prod
# Fernet key encrypting store_credentials at rest. Generate your own with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CREDENTIALS_ENCRYPTION_KEY=change-me-in-prod
